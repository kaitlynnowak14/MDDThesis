#####################################
##### Upload & Save PRS Weights #####
#####################################

# File -> Open -> Upload

import os
print(os.getenv('WORKSPACE_BUCKET'))

file_name = "top10k_prs_weights_hg38.txt"
bucket = "fc-secure-6218f59e-a8a7-40a3-96cb-ee93a398ba0b"

# Copy file from workspace to bucket
!gsutil cp {file_name} gs://{bucket}/

# Verify it’s in the bucket
print("Files in your bucket:")
!gsutil ls gs://{bucket}/
